server {
    listen 80;
    server_name _;

    # Para SSL con certbot (descomentar cuando tengas dominio)
    # location /.well-known/acme-challenge/ {
    #     root /var/www/certbot;
    # }

    location / {
        proxy_pass http://web:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    client_max_body_size 5M;
}

# Descomentar cuando tengas certificado SSL:
# server {
#     listen 443 ssl;
#     server_name tu-dominio.cl;
#
#     ssl_certificate /etc/letsencrypt/live/tu-dominio.cl/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/tu-dominio.cl/privkey.pem;
#
#     location / {
#         proxy_pass http://web:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
